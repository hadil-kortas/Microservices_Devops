apiVersion: v1
kind: Service
metadata:
  name: customers
  labels:
      app: customers
spec:
  selector:
    app: customers
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5001
  type: NodePort
    
